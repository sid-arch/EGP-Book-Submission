/*
  EGP Story Submission — configuration

  GitHub Pages hosts this website, but GitHub Pages cannot securely receive
  and store uploaded videos by itself.

  Replace the placeholder below with the secure POST endpoint that will
  receive multipart/form-data with these fields:

  - video
  - storytellerName
  - ageGroup
  - guardianEmail
  - reviewConsent
  - publishingPermission
  - submittedAt
*/

window.EGP_CONFIG = {
  UPLOAD_ENDPOINT: "YOUR-UPLOAD-ENDPOINT-HERE"
};
